// Жест быстрого изменения размера кисти (Brush Size Modifier): дефолты.
// Hot Key, чувствительность (прирост размера на пиксель движения) и
// направление. Настраиваемое — данные, не магия в системе.
export const BRUSH_RESIZE = { key: 'd', sensitivity: 0.05, direction: 'both' };
export const SENS_PRESETS = [0.025, 0.05, 0.1]; // низкая / средняя / высокая
export const DIRECTIONS = ['both', 'horizontal', 'vertical'];
